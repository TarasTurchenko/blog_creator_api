<h2><%= @post.title %></h2>
<% if @post.description.present? %>
  <p><%= @post.description %></p>
<% end %>
<img class="size--max-parent" src="<%= image_path(@post.attrs['thumbnail']) %>" alt="Post thumbnail">