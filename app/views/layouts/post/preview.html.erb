<html lang="en">
  <head>
    <title><%= @post.title %></title>
    <%= stylesheet_link_tag 'post_preview' %>
  </head>

  <body style="<%= wrapper_styles @post %>" data-blog="<%= @post.blog.id %>" data-post="<%= @post.id %>">
    <%= yield %>
  </body>
</html>