localhost {
    proxy /api web:3000 {
        without /api
    }
    proxy /events web:3000 {
        header_upstream Connection {>Connection}
        header_upstream Upgrade {>Upgrade}
    }
    proxy /assets web:3000
    proxy /images web:3000
    proxy / {$CDN_URL}/blogs
}